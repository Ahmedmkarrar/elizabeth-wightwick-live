((function() {
    'use strict';

    angular
        .module('common.services')
        .factory('common.eventService', ['$rootScope', service]);

    function service() {
        return {};
    }
}))();
